# Click’s standing list

Not a phase. Not ordered. Not a V1 gate. Work these during V1 system test or whenever Click picks one up. Leftover product ideas that are not in V1 sit as V2 consideration ([roadmap.md](roadmap.md)).

1. Test TradingView webhooks (live alerts, not only the Positions dummy). See [phase-9.md](phase-9.md) Later.
2. Test all DCA indicator and Trend start triggers (RSI / MACD / EMA / Supertrend, sit vs cross, timeframes). Compare on **Bybit** (same pair and timeframe, last closed candle), not Binance.
3. More testing on **Perps** (ticket), **Perps bots**, and **TradingView Strategy** desks.
4. Discuss multiple pairs on one DCA bot (including virtual amounts / virtual lots). Today one live playbook owns one contract. Two desks cannot share a key or the same venue account. Folders already stamp several pairs as separate bots. Do not build multi-pair, virtual size, or 3Commas-style deals until Click locks that design (V2).
5. Review **near-only live DCA grid**. Today live rests six rungs per pass, nearest first, but far GTC stays on Bybit. Click asked about only placing limits inside a band (e.g. 3%). Prefer a working set (next 4–6 unfilled rungs nearest mark, cancel the rest) over a fixed %. Playbook knob, not a global. Trade-off: less venue load vs missing a fast dump through empty rungs. Shelved 29 Aug 2026.
6. Refine **backtesting flow** (queue, worker drain, replay fields, run detail, save/attach). See [phase-backtesting.md](phase-backtesting.md). Added 30 Aug 2026; restated 1 Sep 2026.
7. Refine **charts and locations** (where charts sit, which page owns them). See [phase-charts.md](phase-charts.md). Added 30 Aug 2026.
8. Further **copy trade** testing (share, catalogue, paper and live follow, fan-out, guards). See [phase-copy-trading.md](phase-copy-trading.md) step 10. Added 1 Sep 2026.
9. Consider making **templates public** later (member-visible catalog, not the old backtest Publish snapshot). Publish snapshot was removed 2 Sep 2026.
10. **Research wizard** (admin `/admin/backtests`): staged sweeps — least-bad start, then filters/Exit-if, then scale, then one idle platform template. Designed 9 Sep 2026; parked the same day. Needs wave 2. See [phase-backtesting.md](phase-backtesting.md).
11. **Bybit TradFi** (discussed 10 Sep 2026; parked the same day). Not a new venue. First cut if restarted: commodity + stock **perps** on existing Perps/DCA desks (`XAUUSDT`, `CLUSDT`, `TSLAUSDT`) after the master account signs Bybit’s TradFi perps terms in their UI. Trade-only keys stay trade-only — do not sign `/v5/user/agreement` from the app. Not MT5 CFDs (forex/indices; no Open API). Not xStocks (spot). Needs `symbolType` grouping plus session hours / weekend freeze / oil rollover / splits; paper and replay assume 24/7 crypto today.
12. **Plan critical issues user notifications** (added 10 Sep 2026; absorbed by notifications step 7 on 16 Sep 2026). Live-desk sync / repeating venue reject / key verify pages the member inbox and operators. See [phase-notifications.md](phase-notifications.md).
13. **Pin production admin receive address in server env** (added 12 Sep 2026). Today sweeps use the public `admin_address` on the billing chain row. For **production only**, add a server env (never `NEXT_PUBLIC_`) and refuse to sweep to any other address so a database edit cannot redirect sweeps. Develop can keep the admin form. Do not store the admin seed or private key. See [phase-membership.md](phase-membership.md).
14. **Public `/affiliates` sell page** (added 14 Sep 2026). Today it is signup plus three bullets. Recruiters need rates by plan, hold days, “paid on membership not PnL,” and a sample earn line. Parked. See [phase-membership.md](phase-membership.md).
15. **Domains** (noted 17 Sep 2026). Likely **alphadesks.app** for this app. Hold the rest for later landing pages — do not wire or rebrand until Click says: `alphadesks.au`, `alphadesks.com.au`, `signalbots.au`, `signalbots.app`, `tradingbots.run`.
